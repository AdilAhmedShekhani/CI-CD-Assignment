name: Run Script Workflow

on: [push]

jobs:
  run-script-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Run the script
        run: ./run.sh
