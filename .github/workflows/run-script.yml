name: Run Script Workflow

on: [push]

jobs:
  run-script-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Give execute permission to run.sh
        run: chmod +x run.sh

      - name: Run script
        run: ./run.sh
